"""Human-in-the-loop approval gate."""

# TODO: HITLGate class
#   submit(alert) → AlertStatus — auto-approve low severity or queue for human
#   approve(alert_id) → None
#   reject(alert_id) → None
#   _timeout_watcher() — background task, expire pending after timeout
#   is_critical(alert) → bool
