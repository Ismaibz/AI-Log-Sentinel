"""Rate limiter for API calls."""

# TODO: RateLimiter class
#   __init__(max_calls, period)
#   async acquire() → None — block until a slot is available
#   Token bucket or sliding window implementation
