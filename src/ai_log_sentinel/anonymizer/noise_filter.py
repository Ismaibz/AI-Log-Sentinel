"""Noise filter — flags non-security-relevant log entries."""

# TODO: NoiseFilter class
#   __init__(config) — static_extensions, health_paths, ignore_status_codes
#   is_noise(entry: LogEntry) → tuple[bool, str | None]
