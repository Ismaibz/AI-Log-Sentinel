"""PII detection patterns for anonymization."""

# TODO: PIIPattern dataclass (name, regex, token_prefix, description)
# TODO: DEFAULT_PATTERNS list — ipv4, ipv6, email, url_sensitive, path_ids
# TODO: load_patterns(config) → list[PIIPattern]
