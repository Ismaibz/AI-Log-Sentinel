"""Alert model for threat notifications and mitigation tracking."""

# TODO: AlertStatus enum (pending, approved, rejected, expired, executed, failed)
# TODO: Alert dataclass
#   id (uuid), threat (ThreatAssessment), mitigation_rules, status,
#   created_at, resolved_at, auto_action
