"""Gemini API client wrapper — Flash/Pro selection."""

# TODO: GeminiClient class
#   FLASH_MODEL = "gemini-1.5-flash"
#   PRO_MODEL = "gemini-1.5-pro"
#   __init__(config, secrets) — API key from vibelock-python
#   analyze_flash(prompt, log_batch) → str
#   analyze_pro(prompt, log_batch) → str
#   _call(model, prompt, content) → str — rate limiting, retries, error handling
