"""Threat categorizer — Flash first, escalate to Pro."""

# TODO: ThreatCategorizer class
#   __init__(client: GeminiClient, config)
#   categorize(entries: list[AnonymizedEntry]) → list[ThreatAssessment]
#   _should_escalate(flash_result) → bool
