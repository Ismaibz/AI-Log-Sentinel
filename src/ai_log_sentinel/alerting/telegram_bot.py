"""Telegram bot integration."""

# TODO: TelegramDispatcher(AlertDispatcher)
#   Send formatted alerts with MarkdownV2
#   Inline keyboard: Approve/Reject buttons
#   Callback data: "approve:{alert_id}" / "reject:{alert_id}"
#   start_polling() — listen for button callbacks
